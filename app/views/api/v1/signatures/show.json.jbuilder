json.extract! @signature, :content, :user

# json.comments @restaurant.comments do |comment|
#   json.extract! comment, :id, :content
# end

# do something similar to what is above, api on user and within user, I show last signature
